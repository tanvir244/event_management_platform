import LaunchNewEvent from '@/components/LaunchNewEvent/LaunchNewEvent';
import React from 'react';

const page = () => {
    return (
        <div>
            <LaunchNewEvent />
        </div>
    );
};

export default page;